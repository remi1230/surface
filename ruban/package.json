{
  "name": "ruban",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Peindre en allant tout droit — jeu de territoire sur surface parametrique, WebGPU sans moteur.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "measure": "npm run build && node test/phase1.mjs"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "typescript": "^5.9.3",
    "vite": "^7.1.7"
  }
}
